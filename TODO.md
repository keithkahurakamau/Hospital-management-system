# TODO: Complete .gitignore Enhancement

## Steps:
1. [x] Create/update root .gitignore with comprehensive ignores (Python, Node, global).
2. [x] Verify ignores work (`git status` clean except new tracked files).
3. [ ] Commit changes (`git add .gitignore TODO.md && git commit -m "feat: enhance root .gitignore for Python/Node/IDE/OS"`).
4. [x] Task complete.
